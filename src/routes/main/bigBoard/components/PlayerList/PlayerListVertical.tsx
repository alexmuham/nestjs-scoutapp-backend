import React from 'react';
import {View} from 'react-native';
import styles from './PlayerList.style';

const PlayerListVertical: React.FC = () => {
  return <View style={styles.container} />;
};

export default PlayerListVertical;
